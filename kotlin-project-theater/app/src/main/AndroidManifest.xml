<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".data.CinemaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheaterTheme"
        tools:targetApi="31">
        <activity
            android:name=".ui.TicketsListActivity"
            android:exported="true"
            android:label="@string/title_activity_tickets_list"
            android:theme="@style/Theme.AppTheaterTheme" />
        <activity
            android:name=".ui.purchase.TicketActivity"
            android:exported="false"
            android:label="@string/title_activity_ticket"
            android:theme="@style/Theme.AppTheaterTheme" />
        <activity
            android:name=".ui.purchase.CinemasActivity"
            android:exported="false"
            android:label="@string/title_activity_ticket"
            android:theme="@style/Theme.AppTheaterTheme" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="true"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.AppTheaterTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppTheaterTheme">

            <!--
                <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>